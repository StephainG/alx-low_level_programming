#include "main.h"

/**
 * mul - a function that multiplies two integers
 * @a: first input
 * @b: seconf input
 * Return: 0
 */

int mul(int a, int b)
{
	int num = mul(34, 56);
	printf("%d \n", num);
}
